spring:
  application:
    name: gateway

  cloud:
    gateway:
      routes:
        - id: login_service
          uri: http://localhost:8081  
          predicates:
            - Path=/login/**        
server:
  port: 8080